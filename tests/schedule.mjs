import assert from 'node:assert/strict';import {parseGameDate,parseStartTime,opponentFromSpeech} from '../dist/schedule.js';
const now=new Date(2026,8,12,14);assert.equal(parseGameDate('tomorrow',now),'2026-09-13');assert.equal(parseGameDate('Friday',now),'2026-09-18');assert.equal(parseGameDate('September eighteenth 2026',now),'2026-09-18');assert.equal(parseGameDate('September twenty first',now),'2026-09-21');assert.equal(parseGameDate('January 4',now),'2027-01-04');assert.throws(()=>parseGameDate('February 30',now));assert.equal(parseStartTime('seven PM'),'19:00');assert.equal(parseStartTime('seven thirty PM'),'19:30');assert.equal(parseStartTime('12 AM'),'00:00');assert.equal(parseStartTime('no time'),'');assert.throws(()=>parseStartTime('seven'));assert.equal(opponentFromSpeech('We are playing Tigers.'),'Tigers');console.log('PASS spoken dates, relative weekdays, optional times, invalid dates and ambiguous AM/PM');
